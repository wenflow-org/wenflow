# 编辑工作台 (PromptWorkbench) 层级结构分析

## 📊 当前结构

```
编辑工作台 Tab
│
├── 顶部：4 步骤指示器（横向）
│   ├── [1] 编辑 ← 当前
│   ├── [2] 编译
│   ├── [3] 审核
│   └── [4] 发布
│
└── 主体内容（根据当前步骤显示）
    │
    ├─── Step 1: 编辑
    │    │
    │    ├── 标题：1. 编辑 Prompt
    │    │   └── 提示：选择编辑范式...
    │    │
    │    ├── 编辑范式选择（2 个大卡片）
    │    │   ├── [源文件编辑] 推荐·规范化
    │    │   └── [直接编辑] 高级·自由编辑
    │    │
    │    └── 编辑器区域（根据范式显示）
    │        │
    │        ├── 源文件编辑 (paradigm = constrained)
    │        │   ├── 头部
    │        │   │   ├── 标题：源文件编辑器
    │        │   │   └── [查看编译约定] 按钮
    │        │   │
    │        │   └── 内容
    │        │       ├── 如果无源文件
    │        │       │   ├── 警告：此 Skill 暂无源文件
    │        │       │   ├── [创建源文件模板] 按钮
    │        │       │   └── [或切换到直接编辑] 按钮
    │        │       │
    │        │       └── 如果有源文件
    │        │           ├── 工具栏：[保存修改] 按钮 + 修改计数
    │        │           └── SourceView 组件
    │        │               ├── DEFINITIONS 区块
    │        │               │   ├── Identity
    │        │               │   ├── Input
    │        │               │   ├── Output Schema
    │        │               │   └── Stages
    │        │               └── EXECUTION 区块
    │        │                   ├── Format
    │        │                   ├── Context Handling
    │        │                   ├── Stage Logic
    │        │                   └── Constraints
    │        │
    │        └── 直接编辑 (paradigm = direct)
    │            ├── 头部
    │            │   ├── 标题：直接编辑
    │            │   └── 视图切换（Radio 按钮组）
    │            │       ├── [源文本]
    │            │       └── [字段表]
    │            │
    │            └── 内容
    │                ├── 源文本模式
    │                │   ├── 工具栏
    │                │   │   ├── [插入字段引用] 按钮
    │                │   │   ├── [放弃改动] 按钮
    │                │   │   └── [保存并编译] 按钮
    │                │   ├── 文本编辑器（textarea）
    │                │   └── 底部状态条
    │                │       ├── 字符数
    │                │       ├── 源 hash
    │                │       └── 未保存提示
    │                │
    │                └── 字段表模式
    │                    └── FieldTableEditor 组件
    │
    ├─── Step 2: 编译
    │    │
    │    ├── 标题：2. 编译
    │    │   └── 提示：根据范式自动选择编译方式
    │    │
    │    └── 内容（根据范式显示）
    │        │
    │        ├── 源文件模式 → LLM 编译
    │        │   ├── 状态显示
    │        │   │   ├── 正在编译...
    │        │   │   ├── 编译成功（统计信息）
    │        │   │   ├── 编译错误
    │        │   │   └── 未执行
    │        │   │
    │        │   └── 底部按钮
    │        │       ├── [← 返回编辑]
    │        │       ├── [开始 LLM 编译]
    │        │       └── [查看产物 →]
    │        │
    │        └── 直接编辑模式 → 快速编译
    │            ├── 编译元信息（6 个卡片）
    │            │   ├── 编译状态
    │            │   ├── 应用字段数
    │            │   ├── 是否重写
    │            │   ├── DB 落库时间
    │            │   ├── 源 hash
    │            │   └── context hash
    │            │
    │            └── 底部按钮
    │                ├── [← 返回编辑]
    │                ├── [重新编译]
    │                └── [查看产物 →]
    │
    ├─── Step 3: 审核
    │    │
    │    ├── 标题：3. 审核产物
    │    │   └── 提示：根据范式显示不同视图
    │    │
    │    └── 内容（根据范式显示）
    │        │
    │        ├── 直接编辑模式 → 双栏 diff
    │        │   ├── 左栏：源文件
    │        │   ├── 右栏：编译产物
    │        │   └── 高亮：被重写的部分
    │        │
    │        └── 源文件模式 → 单栏预览
    │            ├── 顶部工具栏
    │            │   ├── 元信息：字符数
    │            │   └── [复制全文] 按钮
    │            └── 内容区：完整 Prompt
    │
    └─── Step 4: 发布
         │
         ├── 标题：4. 发布运行
         │   └── 提示：配置运行时参数，选择发布方式
         │
         ├── 参数配置表单
         │   ├── Temperature（滑块）
         │   ├── Max Tokens（滑块）
         │   ├── Model（下拉选择）
         │   ├── Thinking Mode（下拉选择）
         │   └── Reasoning Effort（下拉选择）
         │
         ├── 产物快照
         │   ├── 顶部：编译模式标签 + 字符数
         │   └── 内容：前 600 字符预览
         │
         ├── 发布警告
         │   └── 提示：将覆盖生产文件，自动备份
         │
         └── 发布按钮（3 个）
             ├── [存为草稿] 新版本，不覆盖 ACTIVE
             ├── [热更换] 原地覆盖 ACTIVE，立即生效
             └── [发布为正式版] 完整发布流程
```

---

## 🔍 层级深度分析

### 当前层级数

```
Tab 层级      第 1 层：编辑工作台
  ↓
步骤层级      第 2 层：4 个步骤（编辑/编译/审核/发布）
  ↓
范式层级      第 3 层：2 种范式（源文件/直接编辑）← Step 1 新增
  ↓
视图层级      第 4 层：2-3 种视图（源文本/字段表/结构化）
  ↓
组件层级      第 5 层：SourceView / FieldTableEditor / 参数表单等
```

**总层级数**：5 层（过深）

---

## ⚠️ 问题识别

### 问题 1：范式选择层增加了层级深度

**症状**：
```
用户路径：
编辑工作台 Tab → 点击 Step 1 → 选择范式 → 选择视图 → 开始编辑
```

**影响**：
- 新增的范式选择增加了一层决策
- 用户需要理解"范式"和"视图"的区别
- 从 4 层增加到 5 层

### 问题 2：概念重叠

**范式 vs 视图**：
```
源文件编辑（范式）
  └── 只有一个视图（SourceView）

直接编辑（范式）
  ├── 源文本（视图）
  └── 字段表（视图）
```

**困惑点**：
- 为什么"源文件编辑"只有一个视图？
- 为什么"直接编辑"有两个视图？
- "源文本"（直接编辑）vs "源文件编辑"有什么区别？

### 问题 3：范式卡片占用空间大

**视觉问题**：
```
┌────────────────────────────────────┐
│  编辑范式选择                       │
├────────────────────────────────────┤
│  [源文件编辑]    [直接编辑]         │
│  推荐·规范化     高级·自由编辑      │
│  • 结构化       • 快速迭代          │
│  • 易维护       • 灵活自由          │
│  • 强制约束     • 适合微调          │
└────────────────────────────────────┘
     ↓ 占用了大量垂直空间
┌────────────────────────────────────┐
│  编辑器区域（被挤到下方）            │
└────────────────────────────────────┘
```

**影响**：
- 首屏看不到编辑器
- 需要滚动才能编辑
- 视觉焦点分散

### 问题 4：Step 4 的参数配置与"模型运行时" Tab 重复

**重复配置**：
```
编辑工作台 Step 4
  ├── Temperature
  ├── Max Tokens
  ├── Model
  ├── Thinking Mode
  └── Reasoning Effort

模型运行时 Tab
  ├── Temperature      ← 重复
  ├── Max Tokens       ← 重复
  ├── Model            ← 重复
  ├── Thinking Mode    ← 重复
  └── Reasoning Effort ← 重复
```

**困惑点**：
- 用户不知道该在哪里配置
- 两处配置可能不一致
- 修改后不确定哪个生效

---

## 💡 整合建议

### 建议 1：简化范式选择（推荐）

**方案 A：改为顶部工具栏切换**

```vue
<div class="workbench-toolbar">
  <div class="toolbar-left">
    <el-segmented v-model="paradigm" :options="[
      { label: '源文件编辑', value: 'constrained', icon: Document },
      { label: '直接编辑', value: 'direct', icon: Edit }
    ]" />
  </div>
  
  <div class="toolbar-right" v-if="paradigm === 'direct'">
    <el-radio-group v-model="editMode" size="small">
      <el-radio-button value="text">源文本</el-radio-button>
      <el-radio-button value="fields">字段表</el-radio-button>
    </el-radio-group>
  </div>
</div>
```

**效果**：
```
前 ┌────────────────────────────────────┐
   │  [源文件编辑] [直接编辑]  [源文本] [字段表] │ ← 紧凑的工具栏
   ├────────────────────────────────────┤
   │                                    │
   │                                    │
   │  编辑器区域（占满空间）              │
   │                                    │
   │                                    │
   └────────────────────────────────────┘
后
```

**优势**：
- ✅ 节省垂直空间
- ✅ 编辑器占满屏幕
- ✅ 切换更快捷
- ✅ 视觉层级清晰

---

**方案 B：首次显示卡片，后续记住选择**

```typescript
// 首次访问或无偏好时，显示范式选择卡片
const showParadigmSelector = ref(true)

// 选择后记住偏好
function selectParadigm(p: 'constrained' | 'direct') {
  paradigm.value = p
  showParadigmSelector.value = false
  localStorage.setItem('preferredParadigm', p)
}

// 下次访问直接使用偏好
onMounted(() => {
  const preferred = localStorage.getItem('preferredParadigm')
  if (preferred) {
    paradigm.value = preferred
    showParadigmSelector.value = false
  }
})
```

**效果**：
- 首次访问：显示范式选择卡片（教学）
- 后续访问：直接进入上次选择的范式
- 可以在工具栏切换

**优势**：
- ✅ 保留教学价值
- ✅ 不牺牲效率
- ✅ 记住用户偏好

---

### 建议 2：移除 Step 4 的参数配置

**问题**：与"模型运行时" Tab 功能重复

**解决方案**：

```
Step 4: 发布
  ├── 移除参数配置表单 ❌
  │   ├── Temperature
  │   ├── Max Tokens
  │   └── ...
  │
  ├── 保留产物快照 ✅
  │   ├── 编译模式标签
  │   └── 内容预览
  │
  └── 保留发布按钮 ✅
      ├── [存为草稿]
      ├── [热更换]
      └── [发布为正式版]
```

**添加提示**：
```vue
<el-alert type="info" :closable="false">
  运行时参数（Temperature、Max Tokens 等）请在
  <el-button text type="primary" @click="$emit('switchTab', 'runtime')">
    模型运行时
  </el-button>
  Tab 中配置。
</el-alert>
```

**优势**：
- ✅ 消除重复
- ✅ 职责清晰
- ✅ 减少困惑

---

### 建议 3：优化 Step 指示器

**当前**：4 个横向大卡片，占用空间

**改进**：紧凑的步骤条

```vue
<div class="workbench-stepper">
  <div 
    v-for="(step, idx) in steps" 
    :key="step.key"
    :class="['step-item', {
      'step-item--active': activeStep === step.key,
      'step-item--done': stepDone(step.key)
    }]"
    @click="activeStep = step.key"
  >
    <span class="step-item__number">{{ idx + 1 }}</span>
    <span class="step-item__label">{{ step.label }}</span>
  </div>
</div>
```

**效果**：
```
前 ┌──────┬──────┬──────┬──────┐
   │  1   │  2   │  3   │  4   │  ← 大卡片，高度 ~80px
   │ 编辑 │ 编译 │ 审核 │ 发布 │
   └──────┴──────┴──────┴──────┘

后 ① 编辑 → ② 编译 → ③ 审核 → ④ 发布  ← 紧凑步骤条，高度 ~40px
```

**优势**：
- ✅ 节省空间
- ✅ 视觉更简洁
- ✅ 更像标准步骤条

---

### 建议 4：合并"源文本"和"源文件编辑"的命名

**当前命名混淆**：
```
源文件编辑（范式）      ← DEFINITIONS + EXECUTION 格式
源文本（直接编辑视图）   ← 直接编辑 Prompt 文本
```

**改进命名**：
```
结构化编辑（范式）      ← DEFINITIONS + EXECUTION 格式
文本编辑（直接视图）     ← 直接编辑 Prompt 文本
字段表编辑（直接视图）   ← 字段表形式
```

**或者**：
```
Lab 模式（范式）        ← 使用 Lab 的编译流程
Pro 模式（范式）        ← 专业用户直接编辑
```

**优势**：
- ✅ 命名更清晰
- ✅ 减少混淆
- ✅ 概念区分明显

---

## 🎯 推荐的整合方案

### 方案：简化 + 优化

#### 1. 顶部结构

```vue
<div class="workbench-header">
  <!-- 紧凑的步骤条 -->
  <div class="workbench-stepper">
    ① 编辑 → ② 编译 → ③ 审核 → ④ 发布
  </div>
  
  <!-- 编辑工具栏（仅 Step 1 显示）-->
  <div v-if="activeStep === 'edit'" class="workbench-toolbar">
    <el-segmented v-model="paradigm" :options="paradigmOptions" />
    <el-radio-group v-if="paradigm === 'direct'" v-model="editMode" size="small">
      <el-radio-button value="text">文本</el-radio-button>
      <el-radio-button value="fields">字段表</el-radio-button>
    </el-radio-group>
    <div class="toolbar-spacer"></div>
    <el-button size="small" text @click="showCompileSpec">查看编译约定</el-button>
  </div>
</div>
```

#### 2. Step 1: 编辑（简化后）

```
移除：范式选择大卡片 ❌
改为：顶部工具栏切换 ✅

编辑器区域：
  ├── 结构化模式（Lab）
  │   └── SourceView 组件（占满空间）
  │
  └── 直接编辑模式
      ├── 文本视图
      └── 字段表视图
```

#### 3. Step 4: 发布（简化后）

```
移除：参数配置表单 ❌
保留：
  ├── 产物快照
  ├── 发布警告
  └── 发布按钮
新增：
  └── 提示："参数配置请前往模型运行时 Tab"
```

---

## 📐 视觉对比

### 整合前（当前）

```
┌────────────────────────────────────┐
│ 步骤指示器（4 个大卡片，~80px）      │
├────────────────────────────────────┤
│ 1. 编辑 Prompt                     │
│ 选择编辑范式：...                   │
├────────────────────────────────────┤
│ ┌────────────┐  ┌────────────┐    │
│ │源文件编辑   │  │直接编辑     │    │
│ │推荐·规范化 │  │高级·自由    │    │ ← 大卡片占用空间
│ │• 结构化    │  │• 快速迭代   │    │
│ │• 易维护    │  │• 灵活自由   │    │
│ └────────────┘  └────────────┘    │
├────────────────────────────────────┤
│ 编辑器区域（需要滚动才能看到）        │ ← 被挤到下方
│ ...                                │
└────────────────────────────────────┘
```

### 整合后（推荐）

```
┌────────────────────────────────────┐
│ ① 编辑 → ② 编译 → ③ 审核 → ④ 发布   │ ← 紧凑步骤条 ~40px
│ [结构化] [直接编辑]  [文本] [字段表] │ ← 工具栏 ~40px
├────────────────────────────────────┤
│                                    │
│                                    │
│  编辑器区域（占满屏幕，立即可见）    │ ← 更多编辑空间
│                                    │
│                                    │
│                                    │
└────────────────────────────────────┘
```

**空间节省**：~200px 垂直空间

---

## 📋 实施计划

### 阶段 1：优化 Step 1 编辑步骤（高优先级）

**改动**：
1. 将范式选择卡片改为顶部 Segmented 切换
2. 将视图切换整合到工具栏
3. 优化步骤指示器为紧凑样式

**工时**：2-3 小时

**文件**：
- `PromptWorkbench.vue`（修改模板和样式）

---

### 阶段 2：简化 Step 4 发布步骤（中优先级）

**改动**：
1. 移除参数配置表单
2. 添加跳转到"模型运行时"的提示
3. 保留产物快照和发布按钮

**工时**：1-2 小时

**文件**：
- `PromptWorkbench.vue`（移除表单部分）

---

### 阶段 3：优化命名和提示（低优先级）

**改动**：
1. 统一命名：源文件编辑 → 结构化编辑
2. 优化提示文案
3. 添加首次使用引导

**工时**：1 小时

---

## 🎯 预期效果

### 用户体验改进

- ✅ 减少决策层级：5 层 → 4 层
- ✅ 增加编辑空间：节省 ~200px 垂直空间
- ✅ 减少困惑：消除重复配置
- ✅ 提高效率：快速切换范式和视图

### 视觉改进

- ✅ 更紧凑的顶部区域
- ✅ 更大的编辑器空间
- ✅ 更清晰的层级关系
- ✅ 更现代的交互方式

### 维护性改进

- ✅ 代码更简洁
- ✅ 职责更清晰
- ✅ 易于扩展

---

## ❓ 需要确认

1. **范式命名**：保持"源文件编辑/直接编辑"还是改为"结构化/直接"？
2. **首次体验**：是否需要保留范式选择卡片作为首次引导？
3. **参数配置**：Step 4 完全移除还是保留只读展示？

---

**要开始实施吗？我建议先从阶段 1（优化 Step 1）开始。**
