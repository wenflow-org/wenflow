version: prompt-lab-manifest/v1
skillId: adaptive-guidance-copy
agentId: skill:adaptive-guidance-copy
name: default-skill-adaptive-guidance-copy
archetype: copywriter
description: 学习状态与路径的动态引导文案生成器
acceptableAgentIds: [skill:adaptive-guidance-copy]
publish: { enabled: true, exportTargets: [platform-prompts] }
runtimeDefaults: { tier: chat, temperature: 0.6, maxTokens: 2000, model: null, thinkingMode: default, reasoningEffort: default }
promptContract:
  version: skill-prompt-contract/v2
  executionMode: llm
  artifactKind: copy
  interactionMode: snapshot
  input: { transport: json, schemaSource: skill-definition }
  output: { media: json, schemaSource: runtime-validator, envelope: adapter }
  context: { envelope: context-envelope/v1, delivery: sidecar, modelExposure: projected }
  failurePolicy: deterministic-fallback
runtimeContract:
  version: prompt-runtime-contract/v1
  contextMode: snapshot-context
  businessState: { domain: guidance, phases: [guidance-generated], defaultPhase: guidance-generated, terminalPhases: [guidance-generated], statusValues: [succeeded, partial, blocked, failed] }
  contextUpdate: { mode: none, stateOwner: none }
  outputEnvelope: adapter
ownership: { tier: production, visibility: internal }
tags: [core-skill, copywriter, v4]
notes: v4 核心文件的契约与运行参数家园。
