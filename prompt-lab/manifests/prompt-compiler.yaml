version: prompt-lab-manifest/v1
skillId: prompt-compiler
agentId: skill:prompt-compiler
name: default-skill-prompt-compiler
archetype: generator
description: 受约束的 Prompt 编译说明生成器
acceptableAgentIds: [skill:prompt-compiler]
publish: { enabled: true, exportTargets: [platform-prompts] }
runtimeDefaults: { tier: chat, temperature: 0.2, maxTokens: 8000, model: null, thinkingMode: default, reasoningEffort: default }
promptContract:
  version: skill-prompt-contract/v2
  executionMode: llm
  artifactKind: compilation
  interactionMode: snapshot
  input: { transport: yaml, schemaSource: external-spec }
  output: { media: markdown, schemaSource: external-spec, envelope: adapter }
  context: { envelope: context-envelope/v1, delivery: sidecar, modelExposure: projected }
  failurePolicy: retry
runtimeContract:
  version: prompt-runtime-contract/v1
  contextMode: snapshot-context
  businessState: { domain: prompt-compiler, phases: [prompt-compiled], defaultPhase: prompt-compiled, terminalPhases: [prompt-compiled], statusValues: [succeeded, partial, blocked, failed] }
  contextUpdate: { mode: none, stateOwner: none }
  outputEnvelope: adapter
ownership: { tier: production, visibility: internal }
tags: [core-skill, generator, v4]
notes: v4 核心文件的契约与运行参数家园。
