# NewAPI 自定义配置

# API 配置
NEWAPI_BASE_URL=http://localhost:3000
NEWAPI_API_KEY=***REVOKED_KEY_REMOVED***

# 可用模型
NEWAPI_MODELS=deepseek-chat,deepseek-think

# 默认模型配置
DEFAULT_MODEL=deepseek-chat
DEFAULT_REASONING_MODEL=deepseek-think
DEFAULT_JUDGE_MODEL=deepseek-think
