# AgentEditor Tab 功能分析与设计建议

## 📋 当前 Tab 结构

```
/admin/skills/:skillId
├── 1️⃣ 编辑工作台 (edit)      ← 默认 tab
├── 2️⃣ AI 起草 (ai)
├── 3️⃣ 试运行 (preview)
├── 4️⃣ 评估 (eval)
├── 5️⃣ 模型运行时 (runtime)   ← 仅 Skill 显示
└── 6️⃣ 工程视图 (engineering)
```

---

## 🔍 各 Tab 功能详解

### 1️⃣ 编辑工作台 (PromptWorkbench)

**核心功能**：Skill Prompt 的四步编辑流程

**组件**：`PromptWorkbench.vue`

**流程**：
```
Step 1: 编辑
  ├── 编辑范式选择（新增 ✨）
  │   ├── 源文件编辑（DEFINITIONS + EXECUTION）
  │   └── 直接编辑（源文本 / 字段表）
  └── 实时保存

Step 2: 编译
  ├── 自动根据范式选择编译方式
  │   ├── 源文件 → LLM 编译
  │   └── 直接编辑 → 快速编译（routing 重写）
  └── 显示编译统计

Step 3: 审核
  ├── 源文件模式：单栏预览
  └── 直接编辑模式：双栏 diff

Step 4: 发布
  ├── 配置运行时参数
  ├── 三种发布方式
  │   ├── 存为草稿
  │   ├── 热更换（原地覆盖 ACTIVE）
  │   └── 发布为正式版（备份 + 归档）
  └── 预览产物快照
```

**优势**：
- ✅ 完整的编辑-编译-审核-发布闭环
- ✅ 支持两种编辑范式
- ✅ 自动化编译
- ✅ 热更换支持快速迭代

**问题**：
- ⚠️ 四步流程略长，新手容易迷失
- ⚠️ 编译和发布步骤可以进一步简化

---

### 2️⃣ AI 起草 (PromptAIDraftPane)

**核心功能**：使用 LLM 生成 Prompt 初稿

**组件**：`PromptAIDraftPane.vue`

**流程**：
```
输入需求描述
  ↓
点击"让 AI 起草"
  ↓
LLM 生成 Prompt 初稿
  ↓
显示在预览区
  ↓
点击"注入到编辑器"
  ↓
自动切换到"编辑工作台"tab
  ↓
Prompt 已填充到编辑器
```

**使用场景**：
- 🆕 新建 Skill，从零开始
- 🔄 重构现有 Skill
- 💡 快速原型验证

**优势**：
- ✅ 降低编写门槛
- ✅ 提供结构化模板
- ✅ 快速启动

**问题**：
- ⚠️ 生成质量依赖 LLM
- ⚠️ 需要后续人工调整
- ⚠️ 与"编辑工作台"有重叠（都是编辑 Prompt）

---

### 3️⃣ 试运行 (PromptPreviewPane)

**核心功能**：单次 Skill 调用测试

**组件**：`PromptPreviewPane.vue`

**流程**：
```
输入 JSON 测试数据
  ↓
选择 Prompt 来源
  ├── ACTIVE（生产版本）
  ├── 草稿版本
  └── 自定义粘贴
  ↓
点击"试运行"
  ↓
调用 Skill 执行
  ↓
显示输出 JSON
  ↓
查看调用日志
```

**使用场景**：
- 🧪 快速验证修改
- 🐛 调试错误
- 📊 对比不同版本

**优势**：
- ✅ 快速反馈
- ✅ 无需部署
- ✅ 支持多版本对比

**问题**：
- ⚠️ 只能测试单次调用
- ⚠️ 无法测试复杂链路
- ⚠️ 缺少上下文环境

---

### 4️⃣ 评估 (PromptEvalPane)

**核心功能**：批量评估 Prompt 质量

**组件**：`PromptEvalPane.vue`

**流程**：
```
管理评估用例集
  ├── 手动创建测试用例
  ├── 从调用日志导入
  └── 编辑 / 删除用例
  ↓
选择 Prompt 来源
  ├── ACTIVE
  ├── 草稿版本
  └── 自定义粘贴
  ↓
点击"跑评估"
  ↓
批量运行所有用例
  ↓
显示结果汇总
  ├── 通过率
  ├── 平均耗时
  └── 失败用例详情
  ↓
保存评估报告
```

**使用场景**：
- 📊 发布前质量把关
- 🔬 A/B 测试
- 📈 性能监控
- 🎯 回归测试

**优势**：
- ✅ 系统化质量保障
- ✅ 支持批量测试
- ✅ 历史记录可追溯

**问题**：
- ⚠️ 需要提前准备评估用例
- ⚠️ 维护成本高
- ⚠️ 不适合快速迭代场景

---

### 5️⃣ 模型运行时 (SkillRuntimeConfigPane)

**核心功能**：配置 Skill 节点级模型参数

**组件**：`SkillRuntimeConfigPane.vue`

**数据源**：`skill_model_configs` 表

**配置项**：
```
模型选择
  ├── deepseek-v4-pro
  ├── deepseek-v3
  └── 其他模型

运行时参数
  ├── temperature (0.0 - 2.0)
  ├── max_tokens (1000 - 64000)
  ├── top_p
  └── frequency_penalty

思考模式（针对 reasoning 模型）
  ├── default
  ├── extended
  └── budget-aware

推理强度
  ├── low
  ├── medium
  └── high
```

**使用场景**：
- ⚙️ 调整 Skill 行为
- 🎛️ 性能优化
- 💰 成本控制
- 🧪 模型对比

**优势**：
- ✅ 独立于 Prompt 内容
- ✅ 可以不重新发布 Prompt
- ✅ 支持不同 Skill 不同配置

**问题**：
- ⚠️ 与"编辑工作台 Step 4"的参数配置有重叠
- ⚠️ 容易造成困惑（该在哪里配置？）

---

### 6️⃣ 工程视图 (PromptEngineeringPane)

**核心功能**：Skill 的技术元信息与诊断

**组件**：`PromptEngineeringPane.vue`

**展示内容**：
```
Skill 基本信息
  ├── agentId
  ├── category
  ├── aliases
  └── ioContractVersion

Prompt 来源
  ├── 文件路径（FILE）
  ├── DB 版本（ACTIVE）
  └── 漂移状态

调用统计
  ├── 总调用次数
  ├── 成功率
  ├── 平均耗时
  └── 最近调用时间

字段契约（I/O Contract）
  ├── Input 字段定义
  ├── Output 字段定义
  └── 依赖关系

隶属关系
  ├── 父 Agent
  └── 监控分组

健康状态
  ├── healthy
  ├── degraded
  └── failed
```

**使用场景**：
- 🔍 诊断问题
- 📊 性能分析
- 🔗 依赖关系查看
- 🛠️ 技术调试

**优势**：
- ✅ 全面的技术视图
- ✅ 适合工程师
- ✅ 一站式诊断

**问题**：
- ⚠️ 对运营人员不友好
- ⚠️ 大部分内容是只读的
- ⚠️ 缺少可操作性

---

## 🎯 设计问题分析

### 问题 1：Tab 过多，职责不清
```
6 个 Tab → 用户不知道该用哪个
```

**症状**：
- 编辑 Prompt：可以在"编辑工作台"、"AI 起草"
- 配置参数：可以在"编辑工作台 Step 4"、"模型运行时"
- 测试 Prompt：可以在"试运行"、"评估"

### 问题 2：工作流割裂
```
AI 起草 → 切到编辑工作台 → 切到试运行 → 切回编辑 → 切到评估
```

**症状**：
- 需要频繁切换 Tab
- 上下文丢失
- 无法并排对比

### 问题 3：高级功能埋藏过深
```
"评估"和"模型运行时"使用频率低，但占据一级 Tab
```

**症状**：
- 新手容易迷失
- 常用功能不突出

### 问题 4：信息密度不均
```
"编辑工作台"：功能复杂，信息密集
"工程视图"：只读信息，可操作性低
```

---

## 💡 重新设计建议

### 方案 A：简化为 3 个核心 Tab

```
/admin/skills/:skillId
├── 📝 编辑 (默认)
│   ├── 编辑范式选择（源文件 / 直接编辑）
│   ├── AI 起草按钮（内嵌，不是独立 Tab）
│   ├── 快速试运行按钮（侧边栏）
│   └── 四步流程（编辑 → 编译 → 审核 → 发布）
│
├── 🧪 测试与评估
│   ├── 单次试运行（原"试运行"）
│   ├── 批量评估（原"评估"）
│   └── 对比视图（新增：对比多个版本）
│
└── ⚙️ 配置与诊断
    ├── 模型运行时配置（原"模型运行时"）
    ├── 工程信息（原"工程视图"）
    └── 健康监控（新增）
```

**优势**：
- ✅ 简化到 3 个主 Tab
- ✅ 职责清晰
- ✅ 减少切换

---

### 方案 B：保持当前结构，优化"编辑工作台"

#### B.1 将 AI 起草集成到编辑工作台

```
编辑工作台 Step 1: 编辑
  ├── 编辑范式选择
  │   ├── 源文件编辑
  │   └── 直接编辑
  │
  ├── 如果是空白 Prompt，显示引导
  │   ┌────────────────────────────────┐
  │   │  🎯 快速开始                    │
  │   ├────────────────────────────────┤
  │   │  [AI 起草] [使用模板] [从零开始]│
  │   └────────────────────────────────┘
  │
  └── 编辑器右侧工具栏
      ├── 💡 AI 建议（实时优化）
      ├── 🧪 快速测试（抽屉）
      └── 📖 查看编译约定
```

**效果**：
- ✅ AI 起草变成辅助功能，不占独立 Tab
- ✅ 减少 Tab 切换
- ✅ 保持编辑上下文

#### B.2 合并"试运行"和"评估"

```
测试 Tab
  ├── 快速测试（单次）
  │   ├── 输入 JSON
  │   ├── 选择版本
  │   └── 查看输出
  │
  └── 批量评估（系统化）
      ├── 评估用例管理
      ├── 批量运行
      └── 结果报告
```

**效果**：
- ✅ 统一测试入口
- ✅ 从 6 个 Tab 减少到 4 个

#### B.3 合并"模型运行时"和"工程视图"

```
配置 Tab
  ├── 模型运行时
  │   ├── 模型选择
  │   ├── 参数配置
  │   └── 思考模式
  │
  ├── 工程信息
  │   ├── Skill 元数据
  │   ├── 调用统计
  │   └── 健康状态
  │
  └── 高级配置
      ├── 字段契约
      ├── 依赖关系
      └── 监控分组
```

**效果**：
- ✅ 统一配置入口
- ✅ 从 4 个 Tab 减少到 3 个

---

### 方案 C：引入工作流模式

```
顶部模式切换
  ├── 🎨 创作模式（默认）
  │   ├── AI 起草
  │   ├── 编辑器
  │   ├── 快速预览
  │   └── 保存草稿
  │
  ├── 🧪 测试模式
  │   ├── 单次测试
  │   ├── 批量评估
  │   ├── 对比分析
  │   └── 性能监控
  │
  └── 🚀 发布模式
      ├── 编译检查
      ├── 审核产物
      ├── 配置参数
      └── 发布上线
```

**优势**：
- ✅ 模式化工作流
- ✅ 隐藏不相关功能
- ✅ 清晰的任务导向

**劣势**：
- ⚠️ 改动较大
- ⚠️ 学习成本

---

## 📊 推荐方案对比

| 方案 | 复杂度 | 改动量 | 用户体验 | 可维护性 |
|------|--------|--------|----------|----------|
| 方案 A | ⭐⭐⭐ | 🔧🔧🔧 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 方案 B | ⭐⭐ | 🔧🔧 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 方案 C | ⭐⭐⭐⭐ | 🔧🔧🔧🔧 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |

---

## 🎯 我的推荐：**方案 B**（渐进式优化）

### 理由

1. **改动量适中**：不需要大规模重构
2. **向后兼容**：不破坏现有使用习惯
3. **立竿见影**：从 6 个 Tab 减少到 3-4 个
4. **易于实施**：分阶段进行

### 实施步骤

#### 阶段 1：合并"试运行"和"评估"
```
测试 Tab
  ├── 快速测试（上半部分）
  └── 批量评估（下半部分，可折叠）
```

**改动**：
- 修改 `AgentEditor.vue` tabs 定义
- 合并 `PromptPreviewPane` 和 `PromptEvalPane` 为一个组件

#### 阶段 2：将 AI 起草集成到编辑工作台
```
编辑工作台
  ├── 顶部工具栏新增"AI 起草"按钮
  └── 点击后弹出抽屉，生成完成后自动填充
```

**改动**：
- 在 `PromptWorkbench.vue` 中嵌入 AI 起草抽屉
- 移除 AI 起草 Tab

#### 阶段 3：合并"模型运行时"和"工程视图"
```
配置 Tab
  ├── 模型运行时（可编辑区域）
  └── 工程信息（只读区域，可折叠）
```

**改动**：
- 合并 `SkillRuntimeConfigPane` 和 `PromptEngineeringPane`

### 最终结构

```
/admin/skills/:skillId
├── 📝 编辑工作台（集成 AI 起草）
├── 🧪 测试（合并试运行 + 评估）
└── ⚙️ 配置（合并运行时 + 工程视图）
```

---

## 🎨 UI 优化建议

### 1. 编辑工作台优化

```vue
<div class="workbench-toolbar">
  <!-- 左侧：主要功能 -->
  <div class="toolbar-left">
    <el-button type="primary" @click="showAIDraft">
      <el-icon><MagicStick /></el-icon>
      AI 起草
    </el-button>
    <el-button @click="showQuickTest">
      <el-icon><VideoPlay /></el-icon>
      快速测试
    </el-button>
  </div>
  
  <!-- 右侧：辅助功能 -->
  <div class="toolbar-right">
    <el-button text @click="showCompileSpec">
      查看编译约定
    </el-button>
  </div>
</div>
```

### 2. 测试 Tab 优化

```vue
<el-card>
  <template #header>
    <div class="test-header">
      <el-segmented v-model="testMode" :options="[
        { label: '快速测试', value: 'quick' },
        { label: '批量评估', value: 'batch' }
      ]" />
    </div>
  </template>
  
  <PromptPreviewPane v-if="testMode === 'quick'" />
  <PromptEvalPane v-else />
</el-card>
```

### 3. 配置 Tab 优化

```vue
<el-tabs>
  <el-tab-pane label="模型运行时" name="runtime">
    <SkillRuntimeConfig />
  </el-tab-pane>
  
  <el-tab-pane label="工程信息" name="engineering">
    <EngineeringInfo />
  </el-tab-pane>
</el-tabs>
```

---

## 📝 总结

### 当前问题
- ❌ Tab 过多（6 个）
- ❌ 职责重叠
- ❌ 工作流割裂

### 推荐方案
- ✅ 方案 B：渐进式优化
- ✅ 减少到 3 个 Tab
- ✅ 集成 AI 起草
- ✅ 合并测试功能
- ✅ 统一配置入口

### 实施优先级
1. **高优先级**：合并"试运行"和"评估"
2. **中优先级**：集成 AI 起草到编辑工作台
3. **低优先级**：合并"模型运行时"和"工程视图"

---

**要开始实施吗？我可以先从阶段 1（合并测试 Tab）开始。**
